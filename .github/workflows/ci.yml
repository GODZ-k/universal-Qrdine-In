# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x, 22.x]

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - name: Install dependencies
      run: npm install
    # - name: Deploy to VPS
    #   run: |
    #     ssh user@194.238.19.145 'cd /var/www/universal-Qrdine-In && git pull origin main && npm install && pm2 restart nodeapp'
    #   env:
    #     SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}  # Add your private key to GitHub secrets
    - name: Set up SSH known_hosts
      run: |
        mkdir -p ~/.ssh
        echo '194.238.19.145 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAY4s0QU1KVUOIYj8MqGsY/1nA/RR5t7TGgqgtVE//bm' >> ~/.ssh/known_hosts
    - name: Deploy to VPS
      run: |
        ssh -i ${{ secrets.SSH_PRIVATE_KEY }} user@194.238.19.145 'cd /var/www/universal-Qrdine-In && git pull origin main && npm install && pm2 restart nodeapp'